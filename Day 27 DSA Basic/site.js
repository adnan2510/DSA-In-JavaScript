let a = 10;
let b = 20;

// Swap Number
// let temp = a;
// a = b;
// b = temp;
// console.log(a , b); 

// console.log(a + b + "sum of " + a + "and" + b); // 30

// let a = prompt("Enter first name: ");


// Swap Number Without using Third variable
  // // 1st Approach
  a = a + b - (b = a);

  // // 2nd Approach
  // [a, b] = [b, a];

  // // 3rd Approach
  // a = a + b;
  // b = a - b;
  // a = a - b;

  // // 4th Approach
  // a = a * b;
  // b = a / b;
  // a = a / b;

  // // 5th Approach
  // a = a ^ b;
  // b = a ^ b;
  // a = a ^ b;

  // // 6th Approach
  // let temp = a;
  // b = a;
  // a = temp;

  // console.log(a , b); 
